server:
  port: 8000
spring:
  application:
    name: provider-server
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7000/eureka/
    fetch-registry: true
    register-with-eureka: true